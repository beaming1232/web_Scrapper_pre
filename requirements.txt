# Pinned dependencies for the backend scraping + AI-rewrite pipeline.
# NOTE: no `anthropic` SDK, no `google-generativeai` SDK either —
# pipeline/rewriter.py calls Gemini's generateContent REST API directly
# over httpx (already listed below), so AI rewriting adds zero new
# dependencies.

# --- HTTP / parsing -----------------------------------------------------
httpx==0.27.2
selectolax==0.3.21
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Database -------------------------------------------------------------
asyncpg==0.29.0
SQLAlchemy==2.0.35
alembic==1.13.3

# --- Scheduler --------------------------------------------------------------
APScheduler==3.10.4

# --- API (Phase 3: read-only frontend-facing layer, api/) -------------------
fastapi==0.141.1
uvicorn[standard]==0.52.2

# --- Config / validation -----------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Utilities -----------------------------------------------------------------
python-slugify==8.0.4
yarl==1.13.1
python-dotenv==1.0.1

# --- Testing -----------------------------------------------------------------
pytest==8.3.3
pytest-asyncio==0.24.0
